{
  "engines": {
    "node": ">=18.12.0",
    "npm": ">=8.0.0"
  },
  "name": "root",
  "private": true,
  "scripts": {
    "test": "cd code/shared && npx jest && npm run test --workspaces --if-present",
    "typecheck": "cd code/shared && npx tsc --noEmit && npm run typecheck --workspaces --if-present",
    "update-graphql": "npm run update-graphql --workspaces --if-present"
  },
  "workspaces": [
    "code/client",
    "code/server"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "ts-jest": "^29.2.5"
  }
}
